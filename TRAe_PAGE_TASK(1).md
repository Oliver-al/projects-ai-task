# Trae 页面开发任务

## 1. 任务目标

根据 `apifox.json` 中的接口定义，在当前项目中新增/改造业务页面。

**核心原则：不要从零设计页面。**

当前项目已经存在成熟页面，请先扫描项目，寻找与本次需求最接近的已有页面，然后：

> 复制已有页面的实现方式 → 修改业务字段 → 对接 Apifox API → 完成当前页面

优先复用已有的页面结构、组件、API 封装、Hooks、Store、Utils、类型、i18n 和样式。

---

## 2. Apifox

项目地址：

https://app.apifox.com/project/8605431

本地接口定义：

```text
./docs/apifox.json
```

**优先以 `apifox.json` 中的实际接口定义为准，不要猜测接口参数和返回结构。**

如果 `apifox.json` 中缺少当前需求所需接口，请搜索当前项目是否已经存在对应 API；如果仍然找不到，不要擅自编造接口。

---

## 3. 开发前必须先分析

开始修改代码之前：

1. 扫描项目目录。
2. 搜索与当前需求最相似的已有页面。
3. 找到最接近的页面后，分析其实现。
4. 分析对应 API、类型、组件、分页、Popup/Modal、状态管理和样式。
5. 分析 `docs/apifox.json` 中的接口。
6. 对比旧页面和新接口，确定哪些代码可以直接复用，哪些需要修改。

### 分析结果

请先输出：

```text
参考页面：
页面路径：

参考 API：
API 文件：

使用组件：
- xxx

分页方式：
xxx

Popup / Modal：
xxx

可以直接复用：
- xxx

需要修改：
- xxx

需要新增：
- xxx
```

**分析完成后再开始修改代码。**

---

## 4. 参考页面选择原则

如果没有明确指定参考页面，请自动搜索项目。

优先级：

1. 页面结构最相似
2. 业务功能最相似
3. API 请求方式最相似
4. 分页方式最相似
5. Popup / Modal / Drawer 使用方式最相似
6. 表单结构最相似

找到后优先复制已有实现，不要重新设计。

---

## 5. API 要求

从 `docs/apifox.json` 中确认：

- HTTP Method
- URL
- Path 参数
- Query 参数
- Body 参数
- Header
- Content-Type
- Response
- 分页参数
- 新增接口
- 编辑接口
- 删除接口
- 详情接口
- 状态修改接口

具体以实际接口定义为准。

如果项目已经存在 API 层，请按照项目现有目录和编码方式新增。

不要把 API 请求全部直接写在页面组件内部，除非项目本身就是这种模式。

---

## 6. TypeScript

根据实际接口结构定义类型。

优先复用项目已有类型。

不要重复定义相同类型。

除非确实无法避免，不要使用 `any`。

---

## 7. 页面实现

页面优先复制参考页面。

保持项目现有：

- 页面布局
- Search
- Filter
- Table / List
- Pagination
- Button
- Form
- Popup
- Modal
- Drawer
- Loading
- Empty
- Error

不要为了当前页面重新设计 UI。

---

## 8. 查询

如果接口支持查询：

- 查询条件
- 查询按钮
- 重置按钮
- 查询参数组装
- 查询接口
- 查询后回到第一页

查询条件变化后：

```text
page = 1
```

具体参数名称以 Apifox 为准。

---

## 9. 分页

如果接口支持分页，按照参考页面现有方式实现。

正确处理：

- 首次加载
- 查询
- 重置
- pageSize 修改
- 页码修改
- 删除当前页最后一条数据
- Loading
- 防止重复请求

---

## 10. 新增

如果需求包含新增：

```text
点击新增
↓
打开现有 Popup / Modal
↓
填写表单
↓
表单校验
↓
调用新增 API
↓
成功
↓
关闭弹窗
↓
刷新列表
↓
成功提示
```

复用项目现有 Popup / Modal / Form。

---

## 11. 编辑

如果需求包含编辑：

```text
点击编辑
↓
打开现有 Popup / Modal
↓
回填数据
↓
修改
↓
校验
↓
调用编辑 API
↓
成功
↓
关闭
↓
刷新列表
```

---

## 12. 删除

如果需求包含删除：

```text
点击删除
↓
使用项目现有 Confirm
↓
确认
↓
调用删除 API
↓
成功
↓
刷新列表
```

不要重新实现 Confirm。

---

## 13. UI

保持参考页面视觉风格一致：

- 字体
- 字号
- 间距
- Button
- Input
- Select
- Table
- Popup
- Modal
- Pagination
- Loading
- Empty
- Hover
- Error

已有样式优先复用。

---

## 14. i18n

如果项目使用 i18n：

新增用户可见文本必须按照现有 i18n 方式处理。

不要直接在页面中大量写死中文或英文。

---

## 15. 异常处理

需要处理：

- API 请求失败
- 网络错误
- 参数错误
- 表单校验失败
- 新增失败
- 编辑失败
- 删除失败
- 空数据
- Loading

优先使用项目已有 Toast / Message / Notification。

---

## 16. 修改范围

只修改当前需求相关代码。

允许：

- 新增页面
- 新增 API
- 新增类型
- 新增 i18n
- 新增当前页面所需样式
- 新增当前页面业务逻辑

谨慎修改：

- 公共组件
- 全局 Store
- 公共 Utils
- 全局样式
- 其他业务页面

如果必须修改公共代码：

1. 先分析影响范围。
2. 保持向后兼容。
3. 不要顺便重构无关代码。

---

## 17. 禁止事项

禁止：

- 不分析现有代码就直接开发
- 不读取 `apifox.json` 就猜接口
- 从零重新设计页面
- 重复封装已有组件
- 重复封装已有 API 请求
- 随意引入第三方依赖
- 大范围重构项目
- 修改无关业务
- 大量使用 `any`
- 擅自修改已有页面行为
- 擅自编造 API 字段

---

## 18. 执行流程

严格按照：

```text
Step 1  扫描项目
   ↓
Step 2  寻找最接近的已有页面
   ↓
Step 3  分析参考页面
   ↓
Step 4  读取 docs/apifox.json
   ↓
Step 5  分析 API
   ↓
Step 6  对比旧页面和新 API
   ↓
Step 7  确定复用 / 修改 / 新增内容
   ↓
Step 8  开始开发
   ↓
Step 9  TypeScript 检查
   ↓
Step 10 运行项目
   ↓
Step 11 检查页面功能
   ↓
Step 12 修复问题
```

---

## 19. 完成后自检

### API

```text
[ ] URL 正确
[ ] Method 正确
[ ] Query 正确
[ ] Body 正确
[ ] Response 正确
[ ] 分页参数正确
```

### 页面

```text
[ ] 页面可以正常打开
[ ] 列表正常
[ ] 查询正常
[ ] 重置正常
[ ] 分页正常
[ ] 新增正常
[ ] 编辑正常
[ ] 删除正常
[ ] Popup / Modal 正常
```

### TypeScript

```text
[ ] 无 TS Error
[ ] 无明显 any
[ ] 类型与 API 一致
```

### UI

```text
[ ] 与参考页面风格一致
[ ] Loading 正常
[ ] Empty 正常
[ ] Error 正常
[ ] i18n 正常
```

---

## 20. 最终汇报

完成后输出：

```text
## 修改文件

- xxx
- xxx
- xxx

## 新增功能

- xxx
- xxx
- xxx

## 参考页面

xxx

## 使用的 API

- xxx
- xxx
- xxx

## 复用的代码

- xxx
- xxx

## 验证结果

TypeScript：通过 / 未通过
API：通过 / 未通过
页面：通过 / 未通过
功能：通过 / 未通过

## 遗留问题

无
```

---

# 最重要的原则

```text
复用 > 新建

遵循项目现有规范 > 自己设计

Apifox 实际定义 > 自己猜测

最小修改 > 大范围重构

先分析 > 再修改
```
